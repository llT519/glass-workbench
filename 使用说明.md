# 玻璃工作台 · 使用说明

一款玻璃拟态风格的个人工作台：深色玻璃仪表盘为骨架、WebGL 流体玻璃指标卡为数据门面、3D 亚克力卡片环为展示模式。手机与电脑双端适配，数据可通过 Supabase 云同步。

## 快速开始

**方式一（推荐）**：双击 `启动工作台.bat`
- 自动启动本地服务并打开浏览器（需要电脑装有 Python 或 Node.js 任一）；
- 窗口中会同时显示手机访问地址（需手机与电脑连同一 WiFi）；
- 关闭该窗口即停止服务（不影响已打开页面使用）。

**方式二**：直接双击 `index.html` 也能打开使用（本地模式完整可用；个别浏览器对 file:// 页面限制较多，推荐方式一）。

> 改动会自动保存到浏览器。清除浏览器数据会丢失本地数据，重要数据请定期「导航 → 数据 → 导出全部数据」。

## 五大分区

| 分区 | 内容 |
| --- | --- |
| 🏠 概览 | 时钟问候、4 张流体玻璃指标卡（今日待办 / 番茄专注 / 待复习单词 / 本月支出）、待办清单、番茄钟、便签 |
| 📚 学习 | 学习进度（打卡与连续天数）、艾宾浩斯背单词（遗忘曲线自动安排复习）、每周时间表 |
| 💰 记账 | 快速记一笔、本月收支统计与分类占比、近 7 日趋势、记录明细 |
| 🎯 目标 | 目标进度追踪、截止日提醒、近 7 天完成量统计 |
| 🧭 导航 | 快捷网址、3D 卡片环入口、数据导出 / 导入 / 清空 |

## 三套玻璃视觉

1. **深色玻璃仪表盘（整页框架）**：⚙ 系统设置 → 「主题换肤」可在 16 套配色间整页换肤（画布、环境光、玻璃染色、强调色同步变化），并可微调色彩强度、环境光、玻璃染色；
2. **流体玻璃指标卡（WebGL）**：概览页 4 张横向宽卡由 canonical 流体玻璃内核实时渲染，青色流质缓慢流动，鼠标扫过会推开流体；渲染质量可在设置中调整为 高 / 均衡 / 省电，不支持 WebGL 的环境自动降级为 CSS 动画；
3. **3D 亚克力卡片环**：导航页点「进入 3D 模式」，全屏体验可拖动旋转、滚轮缩放、点击居中的三维卡片环（卡片文字与配色可在 3D 界面内自定义，Esc 或「返回工作台」退出）。

## 功能速览

- **待办**：输入回车添加，勾选完成；完成后计入「近 7 天完成」统计；
- **番茄钟**：25 分钟专注 / 5 分钟休息（可改），完成自动 +1 并计入统计；
- **便签**：多张便签自动保存；
- **学习进度**：添加项目 → ±10% 调进度 → 每日打卡，自动计算连续打卡天数；
- **背单词**：添加单词与释义后，按艾宾浩斯遗忘曲线（1/2/4/7/15/30/60 天）逐日安排复习；「认识了」进入下一阶段，「忘记了」退回第一阶段，走完 7 个阶段即算掌握；
- **时间表**：一周 7 天时间块，今天一列自动高亮；
- **记账**：支出 / 收入双类型、12 个分类、日期备注，自动汇总本月收支与分类占比；
- **目标**：进度条 + 截止日（剩 3 天内变橙、过期变红）。

## 手机端

- 手机浏览器打开电脑地址后，建议「添加到主屏幕」，即可以全屏 App 形式使用；
- 手机端自动切换为单列卡片流 + 底部标签栏布局，指标卡保持横向宽卡形态；
- 填入相同的云同步配置后，两端数据实时互通（详见《云同步配置指南》）。

## 文件清单

| 文件 | 作用 |
| --- | --- |
| `index.html` | 主页面入口 |
| `styles.css` / `app.js` / `sync.js` | 界面样式 / 功能逻辑 / 云同步层 |
| `vendor/` | 棱镜玻璃调色引擎、流体玻璃引擎等视觉内核 |
| `三维卡片环/index.html` | 3D 卡片环组件（独立自包含页面） |
| `manifest.json` / `sw.js` / 图标 | PWA 支持（添加到主屏幕、离线缓存） |
| `启动工作台.bat` | 一键启动本地服务 |
| `云同步配置指南.md` | Supabase 配置全流程 |
