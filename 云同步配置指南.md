# 云同步配置指南

本工作台的云同步基于 **Supabase 免费版**（无需服务器、无需信用卡）。配置一次，手机和电脑即可自动同步全部数据。

## 同步原理

- 数据先保存在每台设备的浏览器（localStorage，本地优先，离线也能用）；
- 联网且有配置时，每次改动约 1.5 秒后自动推送到云端，并每 12 秒拉取一次其他设备的更新；
- 按「模块」为单位合并（待办、便签、记账……各成一个模块），同一模块两边都有改动时，**时间上后写的一方胜出**；
- 多设备通过同一个**同步码**识别彼此：同步码相同 = 同一个人。

## 第一步：注册并创建项目（约 3 分钟）

1. 打开 [https://supabase.com](https://supabase.com)，点击 **Start your project** 注册（可用 GitHub 账号直接登录）；
2. 登录后点击 **New project** 创建项目：
   - Name：随意，如 `glass-workbench`
   - Database Password：设置一个密码并记住（本工作台用不到，但别弄丢）
   - Region：选离你近的（如 Singapore / Tokyo）
3. 等待 1~2 分钟项目初始化完成。

## 第二步：执行建表 SQL（约 1 分钟）

1. 打开项目页面，左侧菜单点击 **SQL Editor**；
2. 新建查询，粘贴以下 SQL，点击 **Run** 运行：

```sql
-- 玻璃工作台同步表：一个同步码 + 模块名 对应一条数据
create table if not exists public.workbench_sync (
  sync_code  text not null,
  module     text not null,
  payload    jsonb not null default '{}'::jsonb,
  updated_at timestamptz not null default now(),
  primary key (sync_code, module)
);

-- 开启行级安全，并允许匿名键按同步码读写（个人使用场景）
alter table public.workbench_sync enable row level security;

create policy "sync_code_full_access"
  on public.workbench_sync
  for all
  to anon, authenticated
  using (true)
  with check (true);
```

3. 运行成功（显示 `Success. No rows returned` 即可）。

## 第三步：获取 URL 和 Anon Key

1. 左侧菜单点击 **Project Settings**（齿轮）→ **API**；
2. 找到两项并复制：
   - **Project URL**：形如 `https://xxxxxxxx.supabase.co`
   - **anon public** 密钥：一串很长的 `eyJhbGciOi…`（注意是 `anon` / `public` 那一个，不是 `service_role`）

## 第四步：在工作台中填入

1. 打开工作台 → **⚙ 系统设置** → 「云同步（Supabase）」；
2. 依次填入：
   - **项目 URL**：刚才复制的 Project URL
   - **Anon Key**：刚才复制的 anon 密钥
   - **同步码**：自己起一串只有你知道的字符，如 `xiaoming-glass-2024`（**所有设备必须填完全相同的一串**）
3. 点击 **保存并连接**，提示「连接成功」即完成；
4. 手机端打开工作台，进入设置填**完全相同**的三项，数据即刻互通。

## 手机端怎么用

1. 手机与电脑连接**同一个 WiFi**；
2. 双击电脑上的 `启动工作台.bat`，窗口里会显示手机访问地址（形如 `http://192.168.x.x:8765/index.html`）；
3. 手机浏览器打开该地址 → 菜单 → 「添加到主屏幕」，之后像 App 一样点开即用；
4. 在手机端设置里填入相同的 Supabase 三项配置。

> 想让手机在任何网络下都能访问？可以把整个 `玻璃工作台` 文件夹部署到任意静态托管（如 Vercel / Netlify / GitHub Pages），手机通过公网地址访问，云同步配置不变。

## 安全说明

- `anon` 密钥设计上就是可公开的前端密钥，但它**等同于你工作台的门钥匙**：拿到「URL + anon key + 同步码」三者即可读写你的数据。请不要把三者同时发到公开场合；
- 建表 SQL 采用的是「同步码即凭证」的最简策略，适合个人使用。如需更高安全性，可后续在 Supabase 中收紧 RLS 策略；
- 数据在云端以 JSON 形式存储，可随时用工作台「导航 → 数据 → 导出全部数据」获得完整备份。

## 常见问题

| 现象 | 原因与解决 |
| --- | --- |
| 连接失败：HTTP 404 | 表还没建：回到第二步执行 SQL |
| 连接失败：HTTP 401 | Anon Key 填错：确认复制的是 anon/public 密钥且完整无空格 |
| 连接失败：Failed to fetch | URL 填错或断网：确认是 `https://xxx.supabase.co` 形式 |
| 两台设备数据不一致 | 确认两边同步码完全一致；稍等十几秒让轮询同步；或在一台设备设置里点「保存并连接」触发立即拉取 |
| 想强制用某一台的数据 | 在那台设备上随意改动一项（如改一条待办），它会作为最新数据覆盖云端 |
| 同步码能换吗 | 能。填新同步码等于切到一个新的空白空间（旧数据还在旧同步码里） |
